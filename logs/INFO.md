# logs/

Run-time log files land here, one per test session:

```
run_YYYYMMDD_HHMMSS.log
```

Generated automatically by `utilities/logger.py`. This directory is
git-ignored (see root `.gitignore`) except for this README, so log
files themselves are never committed.
