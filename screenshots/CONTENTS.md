# screenshots/

Screenshots captured during test runs (via `BasePage.take_screenshot()`)
are saved here, e.g.:

```
homepage_result.png
login_result.png
signup_result.png
```

This directory is git-ignored (see root `.gitignore`) except for this
README, so screenshot images themselves are never committed.
